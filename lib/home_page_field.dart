import 'package:flutter/material.dart';

class HomePageField extends StatelessWidget {
  const HomePageField({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
